import React from 'react'
import ReactDOM from 'react-dom/client'
import { BarbershopApp } from './app/BarbershopApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BarbershopApp />
  </React.StrictMode>,
)
